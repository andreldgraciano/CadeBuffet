<% if @orders.any? { |order| order.event_day.to_date == @order.event_day.to_date && order != @order && order.status != 'Pedido cancelado' && @order.status != 'Pedido cancelado'} %>
  <h3 style="background-color: yellow;">MAIS DE UM PEDIDO ATIVO PARA O MESMO DIA!</h3>
<% end %>

<% if @order.status == 'Aguardando avaliação do buffet' %>

  <%= render('details_order_buffet_profile') %>
  <h2>TOTAL: R$ <%= @order.total_value %></h2>
  <p>Form of payment: <%= @order.payment %></p>

  <%= render('accept') %>
  <%= button_to 'Cancel', cancel_order_path(@order), method: :post %> <br><br>

<% elsif @order.status == 'Pedido aceito pelo buffet' %>

  <%= render('details_order_buffet_profile') %>

  <p>Order vality: <%= @order.order_vality %></p>
  <h3>Original Price: <%= @order.total_value + @order.discount - @order.surcharge %></h3>
  <h3>Discount: <%= @order.discount %></h3>
  <p><%= @order.discount_description %></p>
  <h3>Surcharge: <%= @order.surcharge %></h3>
  <p><%= @order.surcharge_description %></p>
  <h2>TOTAL: R$ <%= @order.total_value %></h2>
  <p>Form of payment: <%= @order.payment %></p>

  <%= button_to 'Cancel', cancel_order_path(@order), method: :post %> <br><br>

<% elsif @order.status == 'Pedido confirmado pelo cliente' %>

  <%= render('details_order_buffet_profile') %>
  <p>Order vality: <%= @order.order_vality %></p>
  <h3>Original Price: <%= @order.total_value + @order.discount - @order.surcharge %></h3>
  <h3>Discount: <%= @order.discount %></h3>
  <p><%= @order.discount_description %></p>
  <h3>Surcharge: <%= @order.surcharge %></h3>
  <p><%= @order.surcharge_description %></p>
  <h2>TOTAL: R$ <%= @order.total_value %></h2>
  <p>Form of payment: <%= @order.payment %></p>

  <%= button_to 'Cancel', cancel_order_path(@order), method: :post %> <br><br>

<% elsif @order.status == 'Pedido cancelado' %>
  <%= render('details_order_buffet_profile') %>
<% end %>
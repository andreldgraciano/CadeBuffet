<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title>CadeBuffet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <header id="header">
      <div class="header_logo">
        <h1><%= link_to 'CADE BUFFET?', buffets_path %></h1>
        <%= render 'shared/avisos' %>
        <div>
          <%= form_with(model: @event, method: :get, url: buffets_path) do |form| %>
            <%= form.label :query, "Search for:" %>
            <%= form.text_field :query %>
            <%= form.submit "Search" %>
          <% end %>
        </div>
      </div>

      <nav class="header_nav" id="client_session">
        <%= link_to 'Meus Pedidos', orders_path %>
        <p style="font-weight: bold;"><%= current_client.name %></p>
        <%= button_to 'Sign out', destroy_client_session_path, method: :delete %>
      </nav>
    </header>
    <main>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>

      <%= yield %>
    </main>
    <footer>

    </footer>
  </body>
</html>

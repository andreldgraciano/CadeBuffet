<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title>CadeBuffet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <header id="header">
      <div class="header_logo">
        <h1><%= link_to 'CADE BUFFET?', home_buffet_profile_path %></h1>
        
      </div>

      <nav class="header_nav" id="buffet_profile_session">
        <%= link_to 'Orders', orders_path %>
        <% if current_buffet_profile.buffet %>
          <p style="font-weight: bold;"> <%= current_buffet_profile.buffet.brand_name %> </p>
        <% end %>
        <%= button_to 'Sign Out', destroy_buffet_profile_session_path, method: :delete %>
      </nav>
    </header>
    <main>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      
      <%= yield %>
    </main>
    <footer>

    </footer>
  </body>
</html>

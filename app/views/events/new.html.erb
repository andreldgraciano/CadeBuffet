<h1>Register Event</h1>

<% if @event.errors.any? %>
  <p>Check the errors below:</p>
  <ul>
  <% @event.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
<% end %>

<%= render('form') %>